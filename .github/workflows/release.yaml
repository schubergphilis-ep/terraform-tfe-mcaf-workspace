# WARNING: This file is managed automatically and may be overwritten at any time.

name: Release

on:
  push:
    branches: [main, master]

jobs:
  Release:
    uses: schubergphilis-ep/github-terraform-workflows/.github/workflows/module-release.yaml@e7b9fcd9a9b85b5c42e690ac140b9def7bee2212 # v2.1.2
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
